import { QuizQuestion } from './types';

// Per user request, fallback quiz data has been removed to ensure
// all questions are generated in real-time by the AI.
export const FALLBACK_QUIZ_DATA: QuizQuestion[] = [];
